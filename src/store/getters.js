const getters = {
  time: state => state.app.time,
  activeStatus: state => state.app.activeStatus,
  activeTab: state => state.app.activeTab,
  lang: state => state.app.lang
}
export default getters
